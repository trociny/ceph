overrides:
  ceph:
    log-whitelist:
      - overall HEALTH_
      - \(CACHE_POOL_NO_HIT_SET\)
      - \(POOL_APP_NOT_ENABLED\)
tasks:
- workunit:
    clients:
      client.0:
        - rbd/fio_test_rbd_journal_object_max_in_flight_appends.sh
    env:
      RBD_FEATURES: "125"
